---
name: zero-trust-standards
alwaysApply: true
---
1. **Web-First**: You MUST perform a Google Search before citing Python 3.13+, Neo4j 2026.1, or PostgreSQL 18.2 syntax.
2. **Zero-Trust**: Every memory retrieval MUST utilize `verify_passkey_token` middleware.
3. **Modularity**: NEVER mix database logic across `/src` directories.
4. **Citations**: All narrative outputs must return a mathematically retrieved `source_uuid`.
