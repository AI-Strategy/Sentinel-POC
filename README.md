# Sentinel POC
Sentenil POC: graph-based “data substrate” that decouples enterprise data from apps. Ingest dirty Invoice (JSON), PO (CSV), POD (XML); stitch into Transaction objects; detect price/qty/phantom line leaks; emit claim-ready evidence reports w/ file+line traceability.
